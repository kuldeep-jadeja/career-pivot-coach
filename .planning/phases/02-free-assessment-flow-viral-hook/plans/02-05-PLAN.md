---
phase: 02-free-assessment-flow-viral-hook
plan: 05
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - app/_components/emails/AssessmentResults.tsx
  - app/_actions/email.ts
  - app/(assessment)/results/page.tsx
  - app/_components/results/EmailCapture.tsx
  - tests/email/results-email.test.tsx
autonomous: true
requirements:
  - ASSESS-07
  - ASSESS-08

must_haves:
  truths:
    - "User can provide email address on results page"
    - "System sends risk score email via Resend when email submitted"
    - "Email contains risk score, job title, and link to results"
  artifacts:
    - path: "app/_components/emails/AssessmentResults.tsx"
      provides: "React Email template for results"
      exports: ["AssessmentResultsEmail"]
    - path: "app/_actions/email.ts"
      provides: "Server Action for sending results email"
      exports: ["sendAssessmentResults"]
    - path: "app/_components/results/EmailCapture.tsx"
      provides: "Email input form component"
      exports: ["EmailCapture"]
  key_links:
    - from: "app/_actions/email.ts"
      to: "lib/email/resend.ts"
      via: "Resend client import"
      pattern: "import.*resend|Resend.*from"
    - from: "app/_components/results/EmailCapture.tsx"
      to: "app/_actions/email.ts"
      via: "sendAssessmentResults invocation"
      pattern: "sendAssessmentResults"
---

<objective>
Build email capture and delivery for assessment results.

Purpose: Allow users to receive their results via email — enabling re-engagement and providing a touchpoint for future marketing (with consent).

Output: Working email capture with React Email templates delivered via Resend.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-free-assessment-flow-viral-hook/02-RESEARCH.md
@.planning/phases/02-free-assessment-flow-viral-hook/02-UI-SPEC.md

<interfaces>
<!-- Existing email infrastructure from Phase 1 -->
From lib/email/resend.ts:
```typescript
import { Resend } from 'resend';
export const resend = new Resend(process.env.RESEND_API_KEY);
```

<!-- Assessment data structure -->
Assessment record contains:
- id: string
- job_title: string  
- risk_score: number (0-100)
- occupation_code: string
- industry: string
- layer_breakdown: object
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React Email template for assessment results</name>
  <files>
    - app/_components/emails/AssessmentResults.tsx
  </files>
  <read_first>
    - lib/email/templates.ts (existing email template patterns, if any)
    - lib/email/resend.ts (Resend client setup)
    - .planning/phases/02-free-assessment-flow-viral-hook/02-RESEARCH.md (Pattern 6: Email Results Delivery)
    - .planning/phases/02-free-assessment-flow-viral-hook/02-UI-SPEC.md (Email Template Styling)
    - package.json (check if react-email is installed)
  </read_first>
  <action>
Install react-email if not present:
```bash
npm install @react-email/components
```

Create app/_components/emails/AssessmentResults.tsx:
```typescript
import {
  Body,
  Container,
  Head,
  Heading,
  Html,
  Link,
  Preview,
  Section,
  Text,
  Hr,
} from '@react-email/components';

interface AssessmentResultsEmailProps {
  jobTitle: string;
  riskScore: number;
  resultsUrl: string;
  riskLevel: string;
}

const main = {
  backgroundColor: '#f6f9fc',
  fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
};

const container = {
  margin: '0 auto',
  padding: '40px 20px',
  maxWidth: '580px',
};

const heading = {
  fontSize: '24px',
  fontWeight: '600' as const,
  color: '#0f172a',
  marginBottom: '24px',
};

const scoreSection = {
  textAlign: 'center' as const,
  margin: '32px 0',
  padding: '32px',
  backgroundColor: '#ffffff',
  borderRadius: '8px',
  border: '1px solid #e2e8f0',
};

const scoreText = {
  fontSize: '72px',
  fontWeight: '700' as const,
  margin: '0',
  lineHeight: '1',
};

const riskLevelText = {
  fontSize: '20px',
  color: '#64748b',
  margin: '12px 0 0 0',
};

const jobTitleText = {
  fontSize: '18px',
  color: '#475569',
  margin: '16px 0 0 0',
};

const paragraph = {
  fontSize: '16px',
  lineHeight: '26px',
  color: '#374151',
  margin: '16px 0',
};

const list = {
  fontSize: '16px',
  lineHeight: '26px',
  color: '#374151',
  paddingLeft: '20px',
  margin: '16px 0',
};

const button = {
  backgroundColor: '#18181b',
  borderRadius: '8px',
  color: '#ffffff',
  fontSize: '16px',
  fontWeight: '600' as const,
  textDecoration: 'none',
  textAlign: 'center' as const,
  display: 'block',
  padding: '14px 24px',
  margin: '32px auto',
  maxWidth: '280px',
};

const footer = {
  fontSize: '12px',
  color: '#64748b',
  lineHeight: '20px',
  marginTop: '32px',
  textAlign: 'center' as const,
};

const hr = {
  borderColor: '#e2e8f0',
  margin: '32px 0',
};

function getScoreColor(score: number): string {
  if (score <= 20) return '#16a34a'; // green-600
  if (score <= 40) return '#ca8a04'; // yellow-600
  if (score <= 60) return '#ea580c'; // orange-600
  if (score <= 80) return '#dc2626'; // red-600
  return '#b91c1c'; // red-700
}

export function AssessmentResultsEmail({
  jobTitle,
  riskScore,
  resultsUrl,
  riskLevel,
}: AssessmentResultsEmailProps) {
  const scoreColor = getScoreColor(riskScore);

  return (
    <Html>
      <Head />
      <Preview>Your {riskScore}% AI displacement risk score for {jobTitle}</Preview>
      <Body style={main}>
        <Container style={container}>
          <Heading style={heading}>Your AI Risk Assessment Results</Heading>

          <Section style={scoreSection}>
            <Text style={{ ...scoreText, color: scoreColor }}>{riskScore}%</Text>
            <Text style={riskLevelText}>{riskLevel}</Text>
            <Text style={jobTitleText}>{jobTitle}</Text>
          </Section>

          <Text style={paragraph}>
            Your <strong>{jobTitle}</strong> role has a {riskScore}% AI displacement
            risk score based on our 4-layer analysis:
          </Text>

          <ul style={list}>
            <li>Task-level AI exposure analysis</li>
            <li>Automation potential assessment</li>
            <li>Industry adoption speed factor</li>
            <li>Experience protection modifier</li>
          </ul>

          <Link href={resultsUrl} style={button}>
            View Full Results →
          </Link>

          <Hr style={hr} />

          <Text style={footer}>
            This assessment is based on O*NET occupational data and published AI
            exposure research from Eloundou et al. and Felten et al.
            <br />
            <br />
            <Link href="https://unautomatable.ai/help" style={{ color: '#64748b' }}>
              Learn more about our methodology
            </Link>
          </Text>
        </Container>
      </Body>
    </Html>
  );
}

export default AssessmentResultsEmail;
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/_components/emails/AssessmentResults.tsx contains `export function AssessmentResultsEmail`
    - app/_components/emails/AssessmentResults.tsx contains `@react-email/components`
    - app/_components/emails/AssessmentResults.tsx contains `riskScore`
    - app/_components/emails/AssessmentResults.tsx contains `resultsUrl`
    - app/_components/emails/AssessmentResults.tsx contains `getScoreColor`
    - npm run build exits 0
  </acceptance_criteria>
  <done>React Email template renders professional results email with score visualization</done>
</task>

<task type="auto">
  <name>Task 2: Create email Server Action and EmailCapture component</name>
  <files>
    - app/_actions/email.ts
    - app/_components/results/EmailCapture.tsx
  </files>
  <read_first>
    - lib/email/resend.ts (Resend client)
    - app/_components/emails/AssessmentResults.tsx (email template)
    - .planning/phases/02-free-assessment-flow-viral-hook/02-RESEARCH.md (Server Action example)
    - .planning/phases/02-free-assessment-flow-viral-hook/02-UI-SPEC.md (Form states)
  </read_first>
  <action>
Create app/_actions/email.ts:
```typescript
'use server';

import { z } from 'zod';
import { Resend } from 'resend';
import { AssessmentResultsEmail } from '@/app/_components/emails/AssessmentResults';

const resend = new Resend(process.env.RESEND_API_KEY);

const emailSchema = z.object({
  to: z.string().email('Invalid email address'),
  jobTitle: z.string().min(1),
  riskScore: z.number().min(0).max(100),
  assessmentId: z.string().min(1),
});

function getRiskLevel(score: number): string {
  if (score <= 20) return 'Low Risk';
  if (score <= 40) return 'Moderate Risk';
  if (score <= 60) return 'Elevated Risk';
  if (score <= 80) return 'High Risk';
  return 'Very High Risk';
}

export async function sendAssessmentResults(input: z.infer<typeof emailSchema>) {
  try {
    const validated = emailSchema.parse(input);

    const baseUrl = process.env.NEXT_PUBLIC_BASE_URL || 'https://unautomatable.ai';
    const resultsUrl = `${baseUrl}/results/${validated.assessmentId}`;
    const riskLevel = getRiskLevel(validated.riskScore);

    const { data, error } = await resend.emails.send({
      from: 'Unautomatable <hello@unautomatable.ai>',
      to: validated.to,
      replyTo: 'support@unautomatable.ai',
      subject: `Your ${validated.riskScore}% AI Risk Score - ${validated.jobTitle}`,
      react: AssessmentResultsEmail({
        jobTitle: validated.jobTitle,
        riskScore: validated.riskScore,
        resultsUrl,
        riskLevel,
      }),
    });

    if (error) {
      console.error('Resend error:', error);
      return { success: false, error: 'Failed to send email' };
    }

    return { success: true, messageId: data?.id };
  } catch (error) {
    console.error('Email send error:', error);
    if (error instanceof z.ZodError) {
      return { success: false, error: error.errors[0].message };
    }
    return { success: false, error: 'Failed to send email' };
  }
}
```

Create app/_components/results/EmailCapture.tsx:
```typescript
'use client';

import { useState } from 'react';
import { Button } from '@/app/_components/ui/button';
import { Input } from '@/app/_components/ui/input';
import { Label } from '@/app/_components/ui/label';
import { Mail, Check, Loader2 } from 'lucide-react';
import { sendAssessmentResults } from '@/app/_actions/email';
import { toast } from 'sonner';

interface EmailCaptureProps {
  assessmentId: string;
  jobTitle: string;
  riskScore: number;
}

type Status = 'idle' | 'sending' | 'sent' | 'error';

export function EmailCapture({ assessmentId, jobTitle, riskScore }: EmailCaptureProps) {
  const [email, setEmail] = useState('');
  const [status, setStatus] = useState<Status>('idle');

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    
    if (!email.trim()) {
      toast.error('Please enter your email address');
      return;
    }

    setStatus('sending');
    
    try {
      const result = await sendAssessmentResults({
        to: email,
        jobTitle,
        riskScore,
        assessmentId,
      });

      if (result.success) {
        setStatus('sent');
        toast.success('Results sent to your email!');
      } else {
        setStatus('error');
        toast.error(result.error || 'Failed to send email');
      }
    } catch (error) {
      setStatus('error');
      toast.error('Failed to send email. Please try again.');
    }
  };

  if (status === 'sent') {
    return (
      <div className="flex items-center gap-3 p-4 bg-green-50 dark:bg-green-950 rounded-lg border border-green-200 dark:border-green-800">
        <Check className="h-5 w-5 text-green-600" />
        <span className="text-green-700 dark:text-green-300">
          Results sent to {email}
        </span>
      </div>
    );
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="email" className="text-base">
          Get results in your inbox
        </Label>
        <div className="flex gap-2">
          <div className="relative flex-1">
            <Mail className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />
            <Input
              id="email"
              type="email"
              placeholder="you@example.com"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="pl-10 min-h-[44px] text-base"
              disabled={status === 'sending'}
            />
          </div>
          <Button 
            type="submit" 
            disabled={status === 'sending'}
            className="min-h-[44px] px-6"
          >
            {status === 'sending' ? (
              <>
                <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                Sending...
              </>
            ) : (
              'Send'
            )}
          </Button>
        </div>
        <p className="text-xs text-muted-foreground">
          We'll send your risk score and a link to view your full results.
        </p>
      </div>
    </form>
  );
}
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/_actions/email.ts contains `'use server'`
    - app/_actions/email.ts contains `resend.emails.send`
    - app/_actions/email.ts contains `AssessmentResultsEmail`
    - app/_components/results/EmailCapture.tsx contains `sendAssessmentResults`
    - app/_components/results/EmailCapture.tsx contains `status === 'sent'`
    - app/_components/results/EmailCapture.tsx contains `min-h-[44px]` (mobile touch target)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Email Server Action sends via Resend; EmailCapture provides form with status feedback</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate EmailCapture into results page and add tests</name>
  <files>
    - app/(assessment)/results/page.tsx
    - tests/email/results-email.test.tsx
  </files>
  <read_first>
    - app/(assessment)/results/page.tsx (current results page)
    - app/_components/results/EmailCapture.tsx (component to integrate)
    - app/_components/emails/AssessmentResults.tsx (template to test)
  </read_first>
  <behavior>
    - Test: AssessmentResultsEmail renders with correct score and job title
    - Test: Email contains link to results URL
    - Test: Score color matches risk band (green for low, red for high)
    - Test: sendAssessmentResults validates email format
  </behavior>
  <action>
Update app/(assessment)/results/page.tsx to include EmailCapture:

Add import at top:
```typescript
import { EmailCapture } from '@/app/_components/results/EmailCapture';
```

Add after ShareButtons section (before closing div):
```typescript
<Card>
  <CardContent className="pt-6">
    <EmailCapture
      assessmentId={assessment.id}
      jobTitle={assessment.job_title}
      riskScore={assessment.risk_score}
    />
  </CardContent>
</Card>
```

Create tests/email/results-email.test.tsx:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render } from '@testing-library/react';

describe('AssessmentResultsEmail', () => {
  it('renders email with correct score and job title', async () => {
    const { AssessmentResultsEmail } = await import(
      '@/app/_components/emails/AssessmentResults'
    );

    const { container } = render(
      <AssessmentResultsEmail
        jobTitle="Software Developer"
        riskScore={65}
        resultsUrl="https://test.com/results/123"
        riskLevel="High Risk"
      />
    );

    // Check score is rendered
    expect(container.textContent).toContain('65%');
    // Check job title is rendered
    expect(container.textContent).toContain('Software Developer');
    // Check risk level is rendered
    expect(container.textContent).toContain('High Risk');
  });

  it('includes link to results URL', async () => {
    const { AssessmentResultsEmail } = await import(
      '@/app/_components/emails/AssessmentResults'
    );

    const { container } = render(
      <AssessmentResultsEmail
        jobTitle="Test Job"
        riskScore={50}
        resultsUrl="https://unautomatable.ai/results/abc123"
        riskLevel="Elevated Risk"
      />
    );

    const link = container.querySelector('a[href*="results/abc123"]');
    expect(link).toBeTruthy();
  });

  describe('getScoreColor', () => {
    // Test color mapping
    const getScoreColor = (score: number): string => {
      if (score <= 20) return '#16a34a'; // green-600
      if (score <= 40) return '#ca8a04'; // yellow-600
      if (score <= 60) return '#ea580c'; // orange-600
      if (score <= 80) return '#dc2626'; // red-600
      return '#b91c1c'; // red-700
    };

    it('returns green for low risk scores', () => {
      expect(getScoreColor(15)).toBe('#16a34a');
    });

    it('returns red for high risk scores', () => {
      expect(getScoreColor(75)).toBe('#dc2626');
    });

    it('returns dark red for very high risk', () => {
      expect(getScoreColor(90)).toBe('#b91c1c');
    });
  });
});

describe('sendAssessmentResults', () => {
  it('validates email format', async () => {
    vi.mock('resend', () => ({
      Resend: vi.fn().mockImplementation(() => ({
        emails: {
          send: vi.fn().mockResolvedValue({ data: { id: 'test-id' } }),
        },
      })),
    }));

    const { sendAssessmentResults } = await import('@/app/_actions/email');

    // Invalid email should fail validation
    const result = await sendAssessmentResults({
      to: 'not-an-email',
      jobTitle: 'Test',
      riskScore: 50,
      assessmentId: '123',
    });

    expect(result.success).toBe(false);
    expect(result.error).toContain('email');
  });
});
```
  </action>
  <verify>
    <automated>npm test tests/email/results-email.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/(assessment)/results/page.tsx contains `EmailCapture`
    - tests/email/results-email.test.tsx contains `describe('AssessmentResultsEmail'`
    - tests/email/results-email.test.tsx contains `renders email with correct score`
    - tests/email/results-email.test.tsx contains `validates email format`
    - npm test tests/email/results-email.test.tsx exits 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>Results page includes email capture; email templates tested for correct rendering</done>
</task>

</tasks>

<verification>
- `npm test tests/email/` — all email tests pass
- `npm run build` — all pages compile
- Manual: Submit email on results page → check inbox for formatted email
</verification>

<success_criteria>
1. EmailCapture form validates email format before submission
2. Server Action sends email via Resend with correct template
3. Email contains risk score, job title, and clickable results link
4. Status feedback shows sending/sent/error states appropriately
5. Email capture integrated into results page after sharing section
</success_criteria>

<output>
After completion, create `.planning/phases/02-free-assessment-flow-viral-hook/plans/02-05-SUMMARY.md`
</output>
